<form id="deleteProfessional{{ professional.id }}" method="post" action="{{ path('admin_user_management_remove_professional', {'id': professional.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional.id) }}">
</form>