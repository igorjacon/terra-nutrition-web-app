<form id="deleteMeal{{ meal.id }}" method="post" action="{{ path('professional_meal_option_remove', {'id': meal.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal.id) }}">
</form>