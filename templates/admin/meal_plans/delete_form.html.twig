<form id="deleteMealPlan{{ mealPlan.id }}" method="post" action="{{ path('professional_meal_plan_remove', {'id': mealPlan.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mealPlan.id) }}">
</form>