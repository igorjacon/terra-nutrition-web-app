<form id="deleteMeasurement{{ measurement.id }}" method="post" action="{{ path('professional_customer_remove_measurement', {'id': measurement.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ measurement.id) }}">
</form>