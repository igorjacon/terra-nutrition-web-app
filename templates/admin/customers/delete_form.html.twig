<form id="deleteCustomer{{ customer.id }}" method="post" action="{{ path('admin_user_management_remove_customer', {'id': customer.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
</form>