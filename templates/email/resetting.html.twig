{% extends "email/base.html.twig" %}

{% block body %}
    <p class="text-muted">
        {{ message|trans({
            '%username%' : username
        })|raw }}
    </p>
    <br>
    <br>
    <p>
        <a href="{{ url }}">
            {{ url }}
        </a>
    </p>
{% endblock %}